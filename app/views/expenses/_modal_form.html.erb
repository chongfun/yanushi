<%# Modal form for creating an expense against a specific rental property %>
<% rental_property ||= expense.rental_property %>

<div class="mb-4 p-3 bg-base-200 rounded-box text-sm">
  <div class="flex justify-between">
    <span class="text-base-content/70">Property</span>
    <span class="font-semibold"><%= rental_property.address %></span>
  </div>
</div>

<%= form_with(model: [rental_property, expense], data: { turbo: true }) do |form| %>
  <% if expense.errors.any? %>
    <div class="alert alert-error mb-4">
      <ul>
        <% expense.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-control w-full mb-4">
    <%= form.label :expense_date, class: "label font-semibold" %>
    <%= form.date_field :expense_date, class: "input input-bordered w-full" %>
  </div>

  <div class="form-control w-full mb-4">
    <%= form.label :category, class: "label font-semibold" %>
    <%= form.select :category, Expense.categories.keys.map { |k| [k.titleize, k] }, { prompt: "Select a category" }, class: "select select-bordered w-full" %>
  </div>

  <div class="form-control w-full mb-4">
    <%= form.label :amount, class: "label font-semibold" %>
    <%= form.number_field :amount, step: "0.01", class: "input input-bordered w-full" %>
  </div>

  <div class="form-control w-full mb-4">
    <%= form.label :description, class: "label font-semibold" %>
    <%= form.text_area :description, class: "textarea textarea-bordered w-full", rows: 2 %>
  </div>

  <div class="divider">Tenant Reimbursement</div>

  <div class="form-control mb-4">
    <label class="label cursor-pointer justify-start gap-4">
      <%= form.check_box :tenant_reimbursable, class: "checkbox checkbox-primary" %>
      <span class="label-text font-semibold">Tenant Reimbursable (Charges Tenant Running Account)</span>
    </label>
  </div>

  <div class="form-control w-full mb-4">
    <%= form.label :reimburse_lease_id, "Lease to Charge", class: "label font-semibold" %>
    <%= form.select :reimburse_lease_id, 
          rental_property.leases.map { |l| ["Lease ##{l.id} (#{l.tenants.pluck(:name).join(', ')})", l.id] }, 
          { include_blank: "Select active lease..." }, 
          { class: "select select-bordered w-full" } %>
  </div>

  <div class="form-control w-full mb-4">
    <%= form.label :reimburse_amount, "Charge Amount (Defaults to expense amount if left blank)", class: "label font-semibold" %>
    <%= form.number_field :reimburse_amount, step: "0.01", class: "input input-bordered w-full", placeholder: "e.g. 150.00" %>
  </div>

  <div class="flex gap-2 mt-6">
    <%= form.submit "Record Expense", class: "btn btn-primary flex-1" %>
    <button type="button" data-action="modal#close" class="btn btn-ghost">Cancel</button>
  </div>
<% end %>
