<% content_for :title, "Editing #{@party.display_name}" %>

<div class="max-w-2xl mx-auto py-6">
  <h1 class="text-2xl font-bold tracking-tight text-stone-900 mb-6">Editing <%= @party.display_name %></h1>
  <%= render "form", party: @party %>
</div>
