<div class="flex justify-center items-center min-h-[70vh]">
  <div class="w-full max-w-md bg-white border border-stone-200 rounded-lg p-8 shadow-sm">
    <h1 class="text-2xl font-bold tracking-tight text-stone-900 text-center mb-6">Forgot password?</h1>
    
    <%= form_with url: passwords_path, class: "space-y-4" do |form| %>
      <div>
        <%= form.label :email, class: "yn-label" %>
        <%= form.email_field :email, required: true, autofocus: true, autocomplete: "username", placeholder: "Enter your email address", value: params[:email], class: "yn-input" %>
      </div>
      
      <div class="pt-2">
        <%= form.submit "Email reset instructions", class: "yn-btn yn-btn-primary w-full" %>
      </div>
    <% end %>
    
    <div class="text-center mt-6">
      <%= link_to "Back to sign in", new_session_path, class: "yn-link text-sm" %>
    </div>
  </div>
</div>
