<% content_for :title, "New lease" %>

<h1 class="text-3xl font-bold mb-6">New lease</h1>

<%= render "form", lease: @lease %>

<div class="mt-6">
  <%= link_to "Back to leases", leases_path, class: "btn btn-ghost" %>
</div>
