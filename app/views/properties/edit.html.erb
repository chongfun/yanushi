<% content_for :title, "Edit #{@property.address}" %>

<div class="max-w-xl">
  <%= render "properties/page_header",
        property: @property,
        title: "Edit property",
        actions: link_to("View property", property_path(@property), class: "yn-btn yn-btn-secondary") %>
  <div class="yn-surface p-6">
    <%= render "form", property: @property %>
  </div>
</div>
