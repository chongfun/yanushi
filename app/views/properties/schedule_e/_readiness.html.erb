<%# Stable aria-live wrapper; Turbo Streams `update` its content so the
    announcement comes from a change inside an existing live region. %>
<p class="mt-1 text-sm text-stone-500" id="schedule_e_readiness" aria-live="polite" aria-atomic="true">
  <%= render "properties/schedule_e/readiness_content" %>
</p>
