<% content_for :title, "Configure tax profile (#{@tax_profile.tax_year})" %>

<div class="max-w-2xl">
  <%= render "properties/page_header",
        property: @property,
        title: "Configure tax profile (#{@tax_profile.tax_year})",
        meta: "Schedule E needs a property type before it can report #{@tax_profile.tax_year}." %>
  <div class="yn-surface p-6">
    <%= render "form", tax_profile: @tax_profile %>
  </div>
</div>
