<% content_for :title, "Edit tax profile (#{@tax_profile.tax_year})" %>

<div class="max-w-2xl">
  <%= render "properties/page_header",
        property: @property,
        title: "Edit tax profile (#{@tax_profile.tax_year})",
        meta: "Applies to the #{@tax_profile.tax_year} Schedule E for this property." %>
  <div class="yn-surface p-6">
    <%= render "form", tax_profile: @tax_profile %>
  </div>
</div>
