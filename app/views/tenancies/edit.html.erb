<% content_for :title, "Editing Tenancy ##{@tenancy.id}" %>

<div class="max-w-2xl mx-auto py-6">
  <h1 class="text-2xl font-bold tracking-tight text-stone-900 mb-6">Editing Tenancy #<%= @tenancy.id %></h1>

  <div class="mb-6">
    <%= render "form", tenancy: @tenancy %>
  </div>
</div>
